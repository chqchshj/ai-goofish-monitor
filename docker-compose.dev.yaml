services:
  app:
    build: .
    image: ${APP_IMAGE:-xianyu-tools:dev}
    container_name: xianyu-tools-dev-app
    init: true
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/dist
    restart: unless-stopped
